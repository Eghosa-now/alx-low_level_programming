#include <stdio.h>

/**
 * main - program to print in reverse function
 * Return: Always 0
 */

int main(void)
{
	char c;

	c = 'z';
	while
		(c >= 'a') {
			putchar(c);
			c--;
			}
	putchar('\n');
	return (0);
}
